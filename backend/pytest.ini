[pytest]
asyncio_mode = auto
pythonpath = .
asyncio_default_fixture_loop_scope = session
markers =
    e2e: End-to-end evaluation tests comparing Gemini models with real API calls
addopts = -m "not e2e"
